import { Shadows } from '@mui/material/styles';

const shadowChannel = '220 30% 5%';
const shadowChannelLight = '220 25% 10%';

export const shadows: Shadows = [
  'none', // 0
  `hsla(${shadowChannel}, 0.07) 0px 4px 16px 0px, hsla(${shadowChannelLight}, 0.07) 0px 8px 16px -5px`, // 1
  `hsla(${shadowChannel}, 0.1) 0px 2px 4px -2px`, // 2
  `hsla(${shadowChannel}, 0.1) 0px 4px 6px -1px`, // 3
  `hsla(${shadowChannel}, 0.1) 0px 6px 8px -1px`, // 4
  `hsla(${shadowChannel}, 0.1) 0px 8px 10px -2px`, // 5
  `hsla(${shadowChannel}, 0.1) 0px 10px 12px -2px`, // 6
  `hsla(${shadowChannel}, 0.1) 0px 12px 14px -3px`, // 7
  `hsla(${shadowChannel}, 0.1) 0px 14px 16px -3px`, // 8
  `hsla(${shadowChannel}, 0.1) 0px 16px 18px -4px`, // 9
  `hsla(${shadowChannel}, 0.1) 0px 18px 20px -4px`, // 10
  `hsla(${shadowChannel}, 0.1) 0px 20px 22px -5px`, // 11
  `hsla(${shadowChannel}, 0.1) 0px 22px 24px -5px`, // 12
  `hsla(${shadowChannel}, 0.1) 0px 24px 26px -6px`, // 13
  `hsla(${shadowChannel}, 0.1) 0px 26px 28px -6px`, // 14
  `hsla(${shadowChannel}, 0.1) 0px 28px 30px -7px`, // 15
  `hsla(${shadowChannel}, 0.1) 0px 30px 32px -7px`, // 16
  `hsla(${shadowChannel}, 0.1) 0px 32px 34px -8px`, // 17
  `hsla(${shadowChannel}, 0.1) 0px 34px 36px -8px`, // 18
  `hsla(${shadowChannel}, 0.1) 0px 36px 38px -9px`, // 19
  `hsla(${shadowChannel}, 0.1) 0px 38px 40px -9px`, // 20
  `hsla(${shadowChannel}, 0.1) 0px 40px 42px -10px`, // 21
  `hsla(${shadowChannel}, 0.1) 0px 42px 44px -10px`, // 22
  `hsla(${shadowChannel}, 0.1) 0px 44px 46px -11px`, // 23
  `hsla(${shadowChannel}, 0.1) 0px 46px 48px -11px`, // 24
];